<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Hraj" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/background_gradient_light_orange_red</item>
    </style>

    <style name="Theme.Halloween.Tiles" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.Halloween.Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.Halloween" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/background_gradient_light_orange_red</item>
    </style>
</resources>